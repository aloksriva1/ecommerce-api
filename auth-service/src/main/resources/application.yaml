server:
  port: 9091

spring:
  application:
    name: auth-service
#  webflux:
#    base-path: /auth-service   # <-- webflux base path applied correctly

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka