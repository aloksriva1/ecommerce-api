server:
    port: 9092
#    servlet:
#        context-path: /user-service   # <-- context path applied correctly

spring:
    application:
        name: user-service
    webflux:
        base-path: /user-service   # <-- webflux base path applied correctly
    r2dbc:
        url: r2dbc:mysql://localhost:3306/user_db
        username: root
        password: thepower

logging:
    level:
        org.springframework.r2dbc: DEBUG
        io.r2dbc.mysql: DEBUG





    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                format_sql: true
                # dialect: org.hibernate.dialect.MySQL8Dialect

management:
    endpoints:
        web:
            exposure:
                include: "*"  # expose all endpoints, including health, info, etc.
